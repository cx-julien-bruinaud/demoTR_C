# Testing dependencies for ProjectHub Backend
# REQUIRES PYTHON 3.7 or 3.8 (these old versions don't work with Python 3.13)
# Tests use SQLite in-memory database, so PostgreSQL is not required

# Same versions as production
Flask==2.3.2
Flask-SQLAlchemy==2.3.2
SQLAlchemy==1.4.0
Flask-CORS==4.0.0
Flask-JWT-Extended==3.13.1
PyJWT==2.8.0
Werkzeug==2.3.7
python-dotenv==0.10.0
requests==2.20.0
lxml==4.9.3
Pillow==10.0.0
PyYAML==6.0.1
Jinja2==3.1.3
MarkupSafe==2.1.3

# Testing frameworks (older versions for Python 3.7/3.8)
pytest==6.2.5
pytest-cov==3.0.0
pytest-flask==1.2.0

# Note: psycopg2-binary and python-magic are excluded from test requirements
# Tests use SQLite in-memory database instead of PostgreSQL

